[package]
name = "gst-plugin-audiofx"
version = "0.7.2"
authors = ["Sebastian Dröge <sebastian@centricular.com>"]
repository = "https://gitlab.freedesktop.org/gstreamer/gst-plugins-rs"
license = "LGPL-2.1-or-later"
description = "Rust AudioFx Plugin"
edition = "2018"

[dependencies]
gst = { package = "gstreamer", version = "0.17", features = ["v1_16"] }
gst-base = { package = "gstreamer-base", version = "0.17", features = ["v1_16"] }
gst-audio = { package = "gstreamer-audio", version = "0.17", features = ["v1_16"] }
byte-slice-cast = "1.0"
num-traits = "0.2"
once_cell = "1.0"
ebur128 = "0.1"
nnnoiseless = { version = "0.3", default-features = false }
smallvec = "1"
atomic_refcell = "0.1"

[lib]
name = "gstrsaudiofx"
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dev-dependencies]
gst-check = { package = "gstreamer-check", version = "0.17", features = ["v1_18"] }
gst-app = { package = "gstreamer-app", version = "0.17" }

[build-dependencies]
gst-plugin-version-helper = { version = "0.7.0", path="../../version-helper" }

[features]
static = []
capi = []

[package.metadata.capi]
min_version = "0.7.0"

[package.metadata.capi.header]
enabled = false

[package.metadata.capi.library]
install_subdir = "gstreamer-1.0"
versioning = false

[package.metadata.capi.pkg_config]
requires_private = "gstreamer-1.0, gstreamer-base-1.0, gstreamer-audio-1.0, gobject-2.0, glib-2.0, gmodule-2.0"
